---
interface Props {
  className?: string
}

const { className = "" } = Astro.props
---

<div
  class={`mt-8 max-w-2xl space-y-4 text-base text-muted-foreground sm:text-lg ${className}`}
>
  <slot />
</div>
