---
import { cn } from "@/lib/utils.ts"

// Props typ
type Props = {
  className?: string
}

// Props
const { className = "" } = Astro.props
---

<ul class={cn("my-4 flex flex-col gap-3", className)}>
  <slot />
</ul>
