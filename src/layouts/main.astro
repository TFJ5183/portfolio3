---
import "@/styles/global.css"
import { Font } from "astro:assets"
---

<!-- Theme script -->
<script is:inline>
  const getThemePreference = () => {
    if (typeof localStorage !== "undefined" && localStorage.getItem("theme")) {
      return localStorage.getItem("theme")
    }
    return window.matchMedia("(prefers-color-scheme: dark)").matches
      ? "dark"
      : "light"
  }
  const isDark = getThemePreference() === "dark"
  document.documentElement.classList[isDark ? "add" : "remove"]("dark")

  if (typeof localStorage !== "undefined") {
    const observer = new MutationObserver(() => {
      const isDark = document.documentElement.classList.contains("dark")
      localStorage.setItem("theme", isDark ? "dark" : "light")
    })
    observer.observe(document.documentElement, {
      attributes: true,
      attributeFilter: ["class"],
    })
  }
</script>

<!-- Template -->
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>The Koi [dot] dev</title>
    <Font cssVariable="--marker-font" preload={true} />
  </head>
  <body>
    <div class="relative h-screen w-screen overflow-hidden">
      {/*  Background */}
      <div class="absolute inset-0 -z-10">
        <img
          alt="background"
          src="/background2.png"
          class="absolute inset-0 -z-10 h-full w-full object-cover blur-sm select-none"
        />
        <div
          class="absolute inset-0 bg-linear-to-r from-black to-black/25 md:to-transparent lg:via-black/25"
        >
        </div>
      </div>

      {/* Children */}
      <slot />

      {/* Footer / Loading */}
      <div
        class="absolute bottom-8 left-1/2 w-full max-w-2xl -translate-x-1/2 animate-[expand-full_1s_ease-in-out_2s_forwards,to-grayscale_1s_ease-in-out_2s_forwards] px-10"
      >
        <div
          class="mb-2 flex justify-between font-mono text-xs tracking-widest text-muted-foreground uppercase opacity-50"
        >
          <span>System Status: Active</span>
          <span class="animate-[fade-in_0.5s_ease-out_2s_forwards] opacity-0"
            >Thekoi [dot] dev</span
          >
        </div>
        <div
          class="h-1 w-full overflow-hidden rounded-full bg-primary/10 transition-colors duration-1000"
        >
          <div
            class="h-full animate-[fill-up_2s_ease-in-out_forwards] rounded-full bg-primary shadow-[0_0_10px_rgba(var(--primary),0.5)] transition-colors duration-1000"
          >
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
